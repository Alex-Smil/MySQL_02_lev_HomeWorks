Привет, удалось частично сделать выборку по упр.3.1 (т.е. все кроме взаимных лайков)
id пользователя +
имя +
лайков получено +
лайков поставлено +
взаимные лайки -

если не сложно ты мог бы кинуть scrn, того как выглядит выборка в консоли по упр. 3.1.
без самого скрипта, не могу представить как должна выглядеть итоговая табл. в консоли вместе с 
полем взаимные лайки.

У меня пока вот такой скрипт:
SELECT u.*, hl.have_likes , pl.put_likes FROM users AS u 
LEFT JOIN (
	SELECT to_user_id, COUNT(to_user_id) AS have_likes
	FROM likes
	GROUP BY to_user_id
) AS hl ON u.id = hl.to_user_id
LEFT JOIN (
	SELECT from_user_id, COUNT(from_user_id) AS put_likes
	FROM likes
	GROUP BY from_user_id
) AS pl ON u.id = pl.from_user_id
GROUP BY u.id;

ссылка на структуру БД myvk1 на gitHub:
https://bit.ly/2vlvVkB
